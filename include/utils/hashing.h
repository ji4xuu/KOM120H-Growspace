#ifndef HASHING_H
#define HASHING_H

#include <string>

// Deklarasi fungsi untuk membuat hash dari sebuah string (password)
std::string hashPassword(const std::string& password);

#endif // HASHING_H